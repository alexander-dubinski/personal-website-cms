import { project } from '@/schemas/project';
import { career, education } from '@/schemas/resume';
import { quote } from '@/schemas/quote';

export const schemaTypes = [project, education, career, quote];
